Requires Python 2.x and latest pyserial to run.

To install Python:
- Download and install the package from Python.org (please select the option "add it to PATH")
- After installation, run command

	pip install pyserial

in cmd.exe
